apiVersion: apps/v1
kind: Deployment
metadata:
  name: netbox-postgres
  labels:
    app: netbox-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: netbox-postgres
  template:
    metadata:
      labels:
        app: netbox-postgres
    spec:
      volumes:
        - name: postgres
          persistentVolumeClaim:
            claimName: netbox-postgres-pv-claim
      containers:  
      - image: postgres:11.9-alpine
        name: postgres 
        ports:
        - containerPort: 5432
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
        volumeMounts:
          - mountPath: /var/lib/postgresql/data
            name: postgres
        env:
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              key: POSTGRES_DB
              name: netbox-postgres-env
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              key: POSTGRES_USER
              name: netbox-postgres-env      
        - name: POSTGRES_PASSWORD
          valueFrom:
            configMapKeyRef:
              key: POSTGRES_PASSWORD
              name: netbox-postgres-env
          
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: netbox-postgres
  labels:
    app: netbox-postgres
spec:
  clusterIP: None
  ports:
  - name: netbox-postgres
    port: 5432
    targetPort: 5432
  selector:
    app: netbox-postgres

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbox-postgres-pv-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: netbox-postgres-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/k8s/netbox-postgres-db"

